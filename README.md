# SMTP Example

Demonstration of an SMTP server implemented with python's socket library.

## Quickstart

- Run the server

  ```
  python server.py
  ```

- Run the client

  ```
  python client.py
  ```
